/**
 * 1. Create a post api on the server side,
 * 2. Send data from client side via post,
 * 3. Set fetch options(second parameter),
 * 4. Options will have three things:
    i. method
    ii. "Content-Type": "application/json"
    iii. body: JSON.stringify(data)
 * 5. Use express.json() middleware on the server side.
*/